# Frontend

Static frontend files are organized by type:

- `pages/` HTML pages
- `styles/` CSS
- `js/` JavaScript
- `assets/` shared assets (icons)
- `data/` JSON data

Run locally:

```bash
cd frontend
python -m http.server 8080
```

Open:

- `http://127.0.0.1:8080/pages/index.html`
